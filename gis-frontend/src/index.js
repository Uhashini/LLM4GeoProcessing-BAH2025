import React from 'react';
import ReactDOM from 'react-dom/client';
import './App.css'; // or './App.css'
import App from './App'; // or './GISChatbot'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
